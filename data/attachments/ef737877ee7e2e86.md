# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: task-completion.spec.ts >> Conclusão de Tarefa >> persiste o estado de conclusão após recarregar a página
- Location: tests/task-completion.spec.ts:29:7

# Error details

```
Error: expect(locator).toBeChecked() failed

Locator:  getByTestId('task-item').filter({ has: getByTestId('task-title').filter({ hasText: 'Persistir conclusão 1776117192024' }) }).getByTestId('task-checkbox')
Expected: checked
Received: unchecked
Timeout:  5000ms

Call log:
  - Expect "toBeChecked" with timeout 5000ms
  - waiting for getByTestId('task-item').filter({ has: getByTestId('task-title').filter({ hasText: 'Persistir conclusão 1776117192024' }) }).getByTestId('task-checkbox')
    9 × locator resolved to <input type="checkbox" data-testid="task-checkbox"/>
      - unexpected value "unchecked"

```

# Page snapshot

```yaml
- generic [active] [ref=e1]:
  - main [ref=e2]:
    - generic [ref=e3]:
      - generic [ref=e4]:
        - heading "Smart To-Do" [level=1] [ref=e5]
        - paragraph [ref=e6]: Powered by Sinky
      - generic [ref=e7]: 1 tarefa
    - generic [ref=e8]:
      - heading "Gerar tarefas com IA" [level=2] [ref=e9]
      - paragraph [ref=e10]: Descreva um objetivo e a IA irá decompô-lo em subtarefas acionáveis.
      - textbox "Sua API Key do OpenRouter (sk-or-v1-...)" [ref=e11]
      - generic [ref=e12]:
        - 'textbox "Ex: Lançar um novo produto de software" [ref=e13]'
        - button "Gerar tarefas" [ref=e14] [cursor=pointer]
    - generic [ref=e16]:
      - textbox "Adicionar nova tarefa..." [ref=e17]
      - button "Adicionar" [ref=e18] [cursor=pointer]
    - list [ref=e19]:
      - listitem [ref=e20]:
        - checkbox [ref=e21] [cursor=pointer]
        - generic [ref=e22]: Persistir conclusão 1776117192024
        - button [ref=e23] [cursor=pointer]:
          - img [ref=e24]
  - alert [ref=e28]
```

# Test source

```ts
  1   | import { test, expect } from '../fixtures/tasks.fixture';
  2   | 
  3   | test.describe('Conclusão de Tarefa', () => {
  4   |   test.beforeEach(async ({ cleanupTasks }) => {
  5   |     await cleanupTasks();
  6   |   });
  7   | 
  8   |   test('marca tarefa como concluída com feedback visual', async ({
  9   |     page,
  10  |     createTaskViaApi,
  11  |     taskListPage,
  12  |   }) => {
  13  |     const tarefa = await createTaskViaApi(`Concluir tarefa ${Date.now()}`);
  14  |     await page.goto('/');
  15  |     await taskListPage.waitForLoaded();
  16  | 
  17  |     const item = taskListPage.itemByTitle(tarefa.title);
  18  |     const checkbox = taskListPage.checkboxOf(item);
  19  | 
  20  |     await expect(checkbox).not.toBeChecked();
  21  |     await checkbox.click();
  22  |     await expect(checkbox).toBeChecked();
  23  | 
  24  |     // Verificar feedback visual: título deve ter tachado (line-through)
  25  |     const tituloEl = item.getByTestId('task-title');
  26  |     await expect(tituloEl).toHaveCSS('text-decoration-line', 'line-through');
  27  |   });
  28  | 
  29  |   test('persiste o estado de conclusão após recarregar a página', async ({
  30  |     page,
  31  |     createTaskViaApi,
  32  |     taskListPage,
  33  |   }) => {
  34  |     // BUG-001: Este teste deve FALHAR com a implementação atual.
  35  |     // toggleComplete não chama PATCH /tasks/:id, portanto o estado nunca é salvo.
  36  |     const tarefa = await createTaskViaApi(`Persistir conclusão ${Date.now()}`);
  37  |     await page.goto('/');
  38  |     await taskListPage.waitForLoaded();
  39  | 
  40  |     await taskListPage.toggleTask(tarefa.title);
  41  |     await expect(taskListPage.checkboxOf(taskListPage.itemByTitle(tarefa.title))).toBeChecked();
  42  | 
  43  |     // Recarrega e verifica persistência
  44  |     await page.reload();
  45  |     await taskListPage.waitForLoaded();
  46  | 
> 47  |     await expect(taskListPage.checkboxOf(taskListPage.itemByTitle(tarefa.title))).toBeChecked();
      |                                                                                   ^ Error: expect(locator).toBeChecked() failed
  48  |   });
  49  | 
  50  |   test('permite desmarcar (desconcluir) uma tarefa já concluída', async ({
  51  |     page,
  52  |     createTaskViaApi,
  53  |     taskListPage,
  54  |   }) => {
  55  |     const tarefa = await createTaskViaApi(`Toggle duplo ${Date.now()}`);
  56  |     await page.goto('/');
  57  |     await taskListPage.waitForLoaded();
  58  | 
  59  |     const item = taskListPage.itemByTitle(tarefa.title);
  60  |     const checkbox = taskListPage.checkboxOf(item);
  61  | 
  62  |     // Conclui
  63  |     await checkbox.click();
  64  |     await expect(checkbox).toBeChecked();
  65  | 
  66  |     // Desconclui
  67  |     await checkbox.click();
  68  |     await expect(checkbox).not.toBeChecked();
  69  |   });
  70  | 
  71  |   test('persiste o estado de desconclusão após recarregar a página', async ({
  72  |     page,
  73  |     createTaskViaApi,
  74  |     taskListPage,
  75  |   }) => {
  76  |     // BUG-001: Também deve FALHAR — desconcluir também não é persistido.
  77  |     const tarefa = await createTaskViaApi(`Persistir desconclusão ${Date.now()}`);
  78  |     await page.goto('/');
  79  |     await taskListPage.waitForLoaded();
  80  | 
  81  |     const item = taskListPage.itemByTitle(tarefa.title);
  82  |     const checkbox = taskListPage.checkboxOf(item);
  83  | 
  84  |     await checkbox.click(); // conclui
  85  |     await checkbox.click(); // desconclui
  86  |     await expect(checkbox).not.toBeChecked();
  87  | 
  88  |     await page.reload();
  89  |     await taskListPage.waitForLoaded();
  90  | 
  91  |     await expect(taskListPage.checkboxOf(taskListPage.itemByTitle(tarefa.title))).not.toBeChecked();
  92  |   });
  93  | 
  94  |   test('tarefa concluída exibe cor de texto esmaecida', async ({
  95  |     page,
  96  |     createTaskViaApi,
  97  |     taskListPage,
  98  |   }) => {
  99  |     const tarefa = await createTaskViaApi(`Estilo visual ${Date.now()}`);
  100 |     await page.goto('/');
  101 |     await taskListPage.waitForLoaded();
  102 | 
  103 |     const item = taskListPage.itemByTitle(tarefa.title);
  104 |     await taskListPage.checkboxOf(item).click();
  105 | 
  106 |     const tituloEl = item.getByTestId('task-title');
  107 |     // Tarefas concluídas devem ter cor cinza esmaecida
  108 |     await expect(tituloEl).toHaveCSS('color', 'rgb(156, 163, 175)');
  109 |   });
  110 | });
  111 | 
```